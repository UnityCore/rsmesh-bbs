TODO for Release 1.1
====================

Status summary (2026-09-12)
  - Core Services Phase A: done (admin toggles, sync/schedule bypass, admin menu gating,
    mesh main menu hides B/C/M when service off; freed keys for modules; optional mail R/S on main menu)
  - Dynamic Main Menu: done (cached mesh_ui/main_menu.txt, promotion, suppress rules,
    per-menu module key validation, deferred admin regen, sysop guide)
  - Admin status displays: done (splash/System Status core services line; tighter banner spacing)
  - Admin System Configuration refactor: done (section menus, required-key seeding)
  - Mesh client virtual radio config: done (config_client.yml server section; sysop guide)
  - Module sync framework Phase A: done (registration API, inbound/outbound dispatch)
  - Module sync framework Phase B: done (per-peer flags, admin, list unsynced)
  - Module sync framework Phase C: done (bbs_list reference module)
  - Release 1.0 -> 1.1 migration: done (release_migration.py; shared 1.1 schema via
    ensure_tc2_upgrade_schema; database_version stamp; config.yml merge; main menu regen)
  - Device Blacklist: not started (moved to 1.2 backlog below)

- Core Services (operator toggles for Bulletins, Mail, Channels) [DONE - Phase A]
    - Admin: Administration -> Core Services (after System Configuration)
      - Toggle each core service on/off (default on for upgrades)
      - Store in sys_config (bbs.core_bulletins, bbs.core_mail, bbs.core_channels);
        seed from config.yml on first run / upgrade
    - When a core service is OFF:
      - Mesh: core handler skipped; matching main-menu key (B/C/M) hidden; freed letter
        available for an enabled module (auto-promoted to main menu without main_menu_visible)
      - Disabled core keys still route to module on_menu_option match, else redraw main menu
        (no "not available" message)
      - Sync outbound: sync_pending_records, sync_*_record, delete sync skip that type
      - Sync inbound: ignore BULLETIN| / MAIL| / CHANNEL| and rsv1 equivalents
      - Schedule: purge_deleted_bulletins / purge_deleted_channels skip when disabled
      - Admin tool: main menu hides Bulletins / Channels / Mail when service off
      - List Unsynced Data omits disabled record types
    - Peer sync peer flags (sync_bulletins, sync_mail, etc.) remain per-peer; core service
      off is a local master switch that overrides outbound/inbound regardless of peer config
    - Core Services option 4: Display Mail commands on Main Menu (bbs.mail_commands_on_main_menu)
      - Off (default): [M]ail on main menu; [R]/[S] on mail submenu
      - On: [R]ead Mail / [S]end Mail on main menu; [M]ail omitted; M does not open mail submenu

- Dynamic Main Menu [DONE]
    - Two-part HELP display: dynamic title line + cached option body (mesh_ui/main_menu.txt)
    - Regenerate cache on core-service / module-flag / suppress toggle; copy prior file to mesh_ui/main_menu.old
    - 1.1 layout: [B]ulletins/[C]hannels, [M]ail/M[o]dules, E[X]IT
    - [M]ail submenu (default): [R]ead Mail, [S]end Mail, E[X]IT — or [R]/[S] on main menu when mail_commands_on_main_menu is on
    - Hide [B]ulletins / [M]ail / [C]hannels on main menu when matching core service is off
    - dispatch_main_menu_key: core handler, main-menu module key, or module override for B/C/M
    - Admin: Administration -> Regenerate Main Menu (manual rewrite of cache)
    - Defer menu regen while inside Administration; single regen on exit / startup
    - Operator edit of main_menu.txt with invalid/oversized fallback to auto-generated body
    - Module keys on main menu (per-module main_menu_visible on modules table)
    - Validate menu_option against main-menu reserved keys only (O/X always; B/C/M when core service on; submenu keys per-menu)
    - Suppress M[O]dules submenu (bbs.suppress_modules_menu):
      - Off (default): M[O]dules lists all enabled modules even if also on main menu
      - On: hide M[O]dules when every enabled module is on main menu; omit duplicates in submenu
      - Admin rejects enabling suppress while any enabled module is not on main menu
    - Hide M[O]dules when there are no enabled modules (suppress ignored)
    - Hide M[O]dules when suppress on and every enabled module is on main menu (disabled modules ignored)
    - docs/RSMESH-BBS-SYSOP-GUIDE.md: mesh main menu, per-menu letters, suppress behavior

- Admin System Configuration refactor [DONE - Phase A]
    - Section menus: BBS, Interface, Schedule + Export at top level
    - Section page header: System Configuration : {section}
    - Numbered key = value rows; select to edit value only (no add/delete)
    - Required keys defined by example_config.yml; seeded into sys_config on startup
    - Missing config.yml keys merged from example_config.yml defaults on startup
    - Export writes schema-ordered bbs/interface/schedule keys to config.yml
    - Core Services / mail layout / suppress_modules_menu excluded from this screen
    - Type-aware editors [DONE]: bool/int/enum/node_id/short_name validation in admin
    - Module sys_config API [DONE]: ctx.register_config / ctx.get_config (module:<dir> only)
    - BBS identity API [DONE]: get_bbs_info / ctx.get_bbs_info; interface radio keys in sys_config

- Mesh client virtual radio config [DONE]
    - config_client.yml server section: virtual_node_id, virtual_short_name, virtual_long_name
    - Defaults: !aabbcc00, BBS0, RSMesh Virtual Radio; node number derived from virtual_node_id
    - client.long_name default: BBS Test Client (example_config_client.yml)
    - rsmesh-bbs_client.py reads server settings; CLI flags override client section only
    - docs/RSMESH-BBS-SYSOP-GUIDE.md: mesh client handset + virtual radio identity

- Module sync capability (framework extension) [PHASE A/B DONE]
    - Phase A [DONE]:
      - ModuleSyncRegistration + ctx.register_sync / ModuleManager registry
      - Inbound RS wire dispatch from message_processing (module wire_types; rsv1 only)
      - Outbound sync_pending callback from sync_pending_records worker
      - Gating: skip inbound/outbound when module disabled in SysAdmin
      - Helpers: get_pending_sync_peers, mark_sync_peers_synced, reset_record_sync_peers
      - docs/RSMESH-BBS-DEVELOPER-GUIDE.md: Peer sync (Phase A) section
    - Phase B [IN PROGRESS]:
      - B1/B2 [DONE]: sync_peer_modules table; per-peer module sync_out/ingest_in flags;
        wired into peer_sync_enabled / peer_accepts_inbound_sync (no row = allow; tc2 = N/A)
      - B3 [DONE]: admin Add/Edit Sync Peer module sync_out / ingest_in prompts (rsv1 only)
      - B4 [DONE]: List Sync Peers shows module sync restrictions (rsv1; N flags only)
      - B5 [DONE]: list_unsynced hook + List Unsynced Data Modules section
      - Module sync status API + Modules admin screen [DONE]:
        get_module_sync_status / sync_status_lines hook; splash Modules peer count
    - Phase C [DONE]:
      - modules/bbs_list: synced directory of mesh BBS boards (rsv1 BBS_LIST_SYNC wire type)
      - Wire suffix policy [DONE]: {module_dir}_{suffix} expansion; collision rejection;
        record_type module:<module_dir> enforcement; legacy_wire_types for inbound aliases
    - Bulletins, Mail, and Channels remain core sync types (tc2/rsv1 wire formats unchanged)

TODO for Release 1.2
====================

- Device Blacklist
    - Ability to ban user by hex node id for misbehaving users
    - Would be good to send an explanation in a message
    - Would be good to make it a limited period ban (say 1 day, a week, etc) or
      permanent

- MQTT Integration
    - Topic Structure
      - MESH_BBS/{Originating_System_ID}/RS/{Protocol_Version}/{Type}/{json-payload}
      - Can't think of a way to limit visibility for any users that have access to
        the MQTT server.  Could operate with this topic structure and let peers set
        up listeners as "subscribers".  Very different than the point-to-point nature
        of mesh peers.
    - Could be used to enable IP/Internet sync between boards
    - Could allow system operators to decide what {Type}s get published
    - Would be good if a board could publish to multiple MQTT servers

- Event/Calendar Module
    - Optional Module using module framework (depends on 1.1 module sync capability)
    - Used to inform of coming events
    - Track event name, day, month, year, start_time, end_time, location
    - Have optional detail message as free text that mesh user can request when viewing
      event list to be sent as a separate message (could have a website url, etc)
    - Create through mesh and admin tool
    - Cancel (Delete) through mesh by posting hex node id only (user can delete their own)
    - Edit/Delete by admin tool
    - Allow mesh users to set reminder for event.  Give option to send reminder week before,
      day before, both

