TODO for Release 1.1
====================

- Core Services (operator toggles for Bulletins, Mail, Channels)
    - Admin: Administration -> Core Services (after System Configuration)
      - Toggle each core service on/off (default on for upgrades)
      - Store in sys_config (bbs scope) or dedicated table; seed from config.yml on first run
    - When a core service is OFF:
      - Mesh: hide related main-menu key(s); reject or no-op mesh user commands for that service
      - Sync outbound: sync_pending_records / sync_*_record skip that record type
      - Sync inbound: ignore BULLETIN| / MAIL| / CHANNEL| and rsv1 equivalents for disabled
        types (same pattern as peer ingest_bulletins / ingest_channels flags today)
      - Schedule: purge workers and other background jobs skip disabled types (e.g. no
        bulletin purge when bulletins off)
      - Admin tool: hide or disable menus for disabled services (Bulletins, Mail, Channels)
    - Peer sync peer flags (sync_bulletins, sync_mail, etc.) remain per-peer; core service
      off is a local master switch that overrides outbound/inbound regardless of peer config

- Dynamic Main Menu
    - Build on existing build_menu() in command_handlers.py (two-column layout today)
    - Driven by Core Services toggles + enabled modules + suppress/hide Modules rules
    - Two-part display on HELP:
      - Line 1 (fixed format, built at display time): "= {board_name} : {mail_count} Msg(s) ="
        so inbox count stays current without regenerating the cached options file
      - Body (cached): menu option lines read from menu/main_menu.txt (path in sys_config)
    - Cached options file menu/main_menu.txt
      - Contains only the option rows (not the title line); generated from current config
      - Server reads file when showing main menu; no rebuild every request
      - Regenerate when core services, module flags, or layout rules change, or via admin
      - Before overwrite: copy existing menu/main_menu.txt to menu/main_menu.old
      - Operator may edit main_menu.txt for branding/layout; invalid/oversized body
        falls back to auto-generated content (200 bytes per mesh packet; multi-packet OK)
      - Admin: Administration -> Regenerate Main Menu (rewrites cache from current config)
    - Main menu layout change (1.1): free [M] for Mail submenu; Modules uses [O]
      - Top level (example two-column):
          [B]ulletins   [C]hannels
          [M]ail        M[O]dules
          E[X]IT
      - [M]ail submenu (when Mail core service enabled): [R]ead Mail, [S]end Mail, E[X]IT
        (same flows as today; only navigation changes)
      - M[O]dules submenu replaces today's [M]odules entry (key O, label M[o]dules)
    - Module keys on main menu (per-module flag, e.g. main_menu_visible on modules table)
      - Validate menu_option does not collide with core keys B, C, M, O, X (and R/S only
        inside Mail submenu, not on main menu)
      - Dynamic main_menu_handlers dispatch (core handlers + module get_by_menu_option)
    - Suppress M[O]dules submenu (global flag, e.g. bbs.suppress_modules_menu)
      - When on, promoted modules are main-menu only (no duplicate listing in submenu)
    - Hide M[O]dules on main menu when nothing needs the submenu:
      - No enabled modules -> no M[O]dules row
      - All enabled modules on main menu AND suppress_modules_menu on -> no M[O]dules row
      - Show M[O]dules when any enabled module is not on main menu, or when suppress
        is off and at least one module exists (operator may still want the aggregator)
    - Hide [M]ail on main menu when Mail core service is off
    - Bulletin and Channel submenus stay fixed format as in Rel 1.0 when enabled
    - Suggested phasing:
      - Phase A: Core Services toggles, sync/schedule bypass, admin Core Services screen
      - Phase B: cached main menu file + Regenerate Main Menu; runtime handler map
      - Phase C: per-module main_menu_visible, key collision checks, operator file edit

- Module sync capability (framework extension)
    - Bulletins, Mail, and Channels remain core sync types (tc2/rsv1 wire formats unchanged)
    - Modules today: mesh menus, schedules, register_service only — no sync hooks
    - Goal: let enabled modules register optional sync participation without moving core
      services into modules (Channels stays core for 1.1)
    - Module registration (at on_load or via ModuleManager), e.g.:
      - sync_record_type id (module-owned data; may use module-local DB like node_info)
      - rsv1 wire message type(s) and encode/decode handlers (rsv1 first; tc2 optional later)
      - inbound ingest handler (called from message_processing when peer + module enabled)
      - outbound sync handler for pending records (called from sync_pending_records worker)
      - optional delete/reconcile handler if the module needs peer delete semantics
    - Gating:
      - Ignore module sync ingest when module disabled (same idea as core service off)
      - Respect sync peer flags where applicable; module may define peer flag names or use
        a generic modules sync enable on the peer row (design TBD)
    - record_sync_peers / synced status:
      - Extend or generalize beyond SYNCED_TABLES for module record types, or module
        stores sync state in its own DB with a documented contract
    - Admin tool:
      - List Unsynced Data: include module-registered types when module enabled
      - Module admin screens may show module-specific sync status
      - Status/splash counts: optional module hook to contribute lines (or query via service)
    - docs/README-MODULES.md: document sync registration API and first reference module
    - Suggested phasing:
      - Phase A: registration API + dispatch from message_processing / sync_pending_records
      - Phase B: admin List Unsynced integration; peer flag design
      - Phase C: first module using sync (candidate: proof-of-concept module, not core Channels)

- Device Blacklist
    - Ability to ban user by hex node id for misbehaving users
    - Would be good to send an explanation in a message
    - Would be good to make it a limited period ban (say 1 day, a week, etc) or
      permanent

TODO for Release 1.2
====================

- MQTT Integration
    - Topic Structure
      - MESH_BBS/{Originating_System_ID}/RS/{Protocol_Version}/{Type}/{json-payload}
      - Can't think of a way to limit visibility for any users that have access to
        the MQTT server.  Could operate with this topic structure and let peers set
        up listeners as "subscribers".  Very different than the point-to-point nature
        of mesh peers.
    - Could be used to enable IP/Internet sync between boards
    - Could allow system operators to decide what {Type}s get published
    - Would be good if a board could publish to multiple MQTT servers

- Event/Calendar Module
    - Optional Module using module framework (depends on 1.1 module sync capability)
    - Used to inform of coming events
    - Track event name, day, month, year, start_time, end_time, location
    - Have optional detail message as free text that mesh user can request when viewing
      event list to be sent as a separate message (could have a website url, etc)
    - Create through mesh and admin tool
    - Cancel (Delete) through mesh by posting hex node id only (user can delete their own)
    - Edit/Delete by admin tool
    - Allow mesh users to set reminder for event.  Give option to send reminder week before,
      day before, both


