TODO for Release 1.1
====================

- Dynamic Main Menu
    - Build on existing build_menu() in command_handlers.py (two-column layout today)
    - Track valid menu options at runtime (enabled modules, mail count in title, etc.)
    - Generate a generic two-column menu ASCII by default when no operator override
    - Operator custom menu file (optional, e.g. config path or sys_config)
      - Display-only override preferred: cosmetic text; keys/behavior stay in code/DB
      - Enforce mesh packet limit (200 bytes per message); fallback to generated menu
        if missing, invalid, or oversized
      - May use multiple packets (send_user_messages) if needed for longer branding
    - Module keys on main menu (per-module flag, e.g. main_menu_visible on modules table)
      - Validate menu_option does not collide with core keys B, C, R, S, M, X
      - Dynamic main_menu_handlers dispatch (core handlers + module get_by_menu_option)
    - Suppress [M]odules submenu (global flag, e.g. bbs.suppress_modules_menu)
      - When on, promoted modules are main-menu only (no duplicate listing in submenu)
    - Hide [M]odules on main menu when nothing needs the submenu:
      - No enabled modules -> no [M]odules
      - All enabled modules on main menu AND suppress_modules_menu on -> no [M]odules
      - Show [M]odules when any enabled module is not on main menu, or when suppress
        is off and at least one module exists (operator may still want the aggregator)
    - Bulletin, Channel, and Mail submenus stay fixed format as in Rel 1.0
    - Suggested phasing:
      - Phase A: runtime build_menu, hide M when no modules, suppress toggle
      - Phase B: per-module main_menu_visible, dynamic handler table, key collision checks
      - Phase C: operator custom menu file with validation and fallback

- Device Blacklist
    - Ability to ban user by hex node id for misbehaving users
    - Would be good to send an explanation in a message
    - Would be good to make it a limited period ban (say 1 day, a week, etc) or
      permanent

TODO for Release 1.2
====================

- MQTT Integration
    - Topic Structure
      - MESH_BBS/{Originating_System_ID}/RS/{Protocol_Version}/{Type}/{json-payload}
      - Can't think of a way to limit visibility for any users that have access to
        the MQTT server.  Could operate with this topic structure and let peers set
        up listeners as "subscribers".  Very different than the point-to-point nature
        of mesh peers.
    - Could be used to enable IP/Internet sync between boards
    - Could allow system operators to decide what {Type}s get published
    - Would be good if a board could publish to multiple MQTT servers

- Event/Calendar Module
    - Optional Module using module framework
    - Used to inform of coming events
    - Track event name, day, month, year, start_time, end_time, location
    - Have optional detail message as free text that mesh user can request when viewing
      event list to be sent as a separate message (could have a website url, etc)
    - Create through mesh and admin tool
    - Cancel (Delete) through mesh by posting hex node id only (user can delete their own)
    - Edit/Delete by admin tool
    - Allow mesh users to set reminder for event.  Give option to send reminder week before,
      day before, both


